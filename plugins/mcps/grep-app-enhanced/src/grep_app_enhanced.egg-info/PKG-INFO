Metadata-Version: 2.4
Name: grep-app-enhanced
Version: 0.1.0
Summary: 增强版 grep-app MCP 服务器，支持 ZSTD 压缩数据库、远程仓库搜索和智能缓存
Author-email: Oh My LiteCode Team <team@oh-my-litecode.dev>
License: MIT
Project-URL: Homepage, https://github.com/oh-my-litecode/oh-my-litecode
Project-URL: Documentation, https://github.com/oh-my-litecode/oh-my-litecode/tree/main/plugins/mcps/grep-app-enhanced
Project-URL: Repository, https://github.com/oh-my-litecode/oh-my-litecode.git
Project-URL: Issues, https://github.com/oh-my-litecode/oh-my-litecode/issues
Project-URL: Changelog, https://github.com/oh-my-litecode/oh-my-litecode/blob/main/plugins/mcps/grep-app-enhanced/CHANGELOG.md
Keywords: mcp,grep,search,zstd,compression,github,code-search
Classifier: Development Status :: 3 - Alpha
Classifier: Intended Audience :: Developers
Classifier: License :: OSI Approved :: MIT License
Classifier: Operating System :: OS Independent
Classifier: Programming Language :: Python :: 3
Classifier: Programming Language :: Python :: 3.10
Classifier: Programming Language :: Python :: 3.11
Classifier: Programming Language :: Python :: 3.12
Classifier: Topic :: Software Development :: Libraries :: Python Modules
Classifier: Topic :: Utilities
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: mcp>=1.0.0
Requires-Dist: zstandard>=0.21.0
Requires-Dist: aiohttp>=3.9.0
Requires-Dist: beautifulsoup4>=4.12.0
Requires-Dist: httpx>=0.25.0
Requires-Dist: pydantic>=2.0.0
Requires-Dist: rich>=13.0.0
Requires-Dist: click>=8.0.0
Provides-Extra: dev
Requires-Dist: pytest>=7.4.0; extra == "dev"
Requires-Dist: pytest-asyncio>=0.21.0; extra == "dev"
Requires-Dist: pytest-cov>=4.1.0; extra == "dev"
Requires-Dist: mypy>=1.5.0; extra == "dev"
Requires-Dist: ruff>=0.1.0; extra == "dev"
Requires-Dist: black>=23.0.0; extra == "dev"
Requires-Dist: pre-commit>=3.5.0; extra == "dev"
Provides-Extra: docs
Requires-Dist: sphinx>=7.0.0; extra == "docs"
Requires-Dist: sphinx-rtd-theme>=2.0.0; extra == "docs"
Requires-Dist: myst-parser>=2.0.0; extra == "docs"

# Grep App Enhanced

增强版 grep-app MCP 服务器，提供高效的代码搜索功能，支持 ZSTD 压缩数据库、远程仓库搜索和智能缓存。

## 功能特性

- 🔍 **本地搜索**: 高效的本地文件系统搜索，支持正则表达式和多模式匹配
- 🌐 **远程搜索**: 支持 GitHub、GitLab 等远程仓库的代码搜索
- 🗜️ **ZSTD 压缩**: 使用 ZSTD 算法压缩数据库，显著减少存储空间
- 💾 **智能缓存**: 多层缓存管理（内存 + 磁盘），LRU 淘汰策略
- 🔌 **MCP 协议**: 完整的 MCP (Model Context Protocol) 服务器实现
- ⚡ **并行处理**: 多线程/多进程支持，提升搜索性能

## 快速开始

### 安装

```bash
# 克隆仓库
cd plugins/mcps/grep-app-enhanced

# 运行安装脚本
./scripts/install.sh

# 或者手动安装
pip install -e .
```

### 安装依赖

```bash
# 安装 ZSTD 压缩库（必需）
./scripts/setup-zstd.sh

# 手动安装 ZSTD
# macOS: brew install zstd
# Ubuntu: sudo apt install zstd libzstd-dev
# Termux: pkg install zstd libzstd
```

### 运行

```bash
# 直接运行 MCP 服务器
grep-app-enhanced

# 或者使用 Python
python -m grep_app_enhanced.mcp_server

# 带配置运行
GREP_APP_CACHE_DIR=/tmp/cache \
GREP_APP_DB_PATH=/tmp/grep.db \
GITHUB_TOKEN=ghp_xxx \
grep-app-enhanced
```

## 配置

### 环境变量

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| `GREP_APP_CACHE_DIR` | 缓存目录 | 无（禁用磁盘缓存） |
| `GREP_APP_DB_PATH` | 数据库文件路径 | 无（禁用数据库） |
| `GITHUB_TOKEN` | GitHub API Token | 无（使用 gh CLI） |
| `GREP_APP_MAX_WORKERS` | 最大工作线程数 | 4 |

### MCP 工具

服务器提供以下 MCP 工具：

#### search_local

在本地文件系统中搜索代码。

```json
{
  "name": "search_local",
  "arguments": {
    "pattern": "def main",
    "path": "/src",
    "include": ["*.py"],
    "exclude": ["__pycache__", ".git"],
    "use_regex": true,
    "case_sensitive": false,
    "max_results": 100,
    "context_lines": 2
  }
}
```

#### search_remote

在远程 Git 仓库中搜索代码。

```json
{
  "name": "search_remote",
  "arguments": {
    "pattern": "TODO",
    "repo": "owner/repo",
    "platform": "github",
    "ref": "main",
    "path": "src/",
    "language": "Python",
    "max_results": 50
  }
}
```

#### get_cache_stats

获取缓存统计信息。

#### clear_cache

清空缓存。

#### get_db_stats

获取数据库统计信息。

## 使用示例

### Python API

```python
import asyncio
from grep_app_enhanced import GrepAppEnhanced, SearchConfig
from grep_app_enhanced.search import LocalSearch, RemoteSearch
from grep_app_enhanced.database import CompressedDatabase, CacheManager

async def main():
    # 本地搜索
    async with LocalSearch() as search:
        results = await search.search(
            pattern=r"def \w+",
            path="/path/to/code",
            include=["*.py"],
            max_results=100
        )
        for result in results:
            print(f"{result.file_path}:{result.line_number}: {result.content}")

    # 远程搜索
    async with RemoteSearch(token="ghp_xxx") as search:
        results = await search.search(
            pattern="def main",
            repo="microsoft/vscode",
            platform="github",
            language="TypeScript"
        )

    # 压缩数据库
    async with CompressedDatabase("cache.db") as db:
        await db.store_search_results(
            query_hash="abc123",
            pattern="test",
            search_path="/src",
            results=results,
            ttl_seconds=3600
        )
        retrieved = await db.retrieve_search_results("abc123")

    # 缓存管理
    async with CacheManager(ttl=3600, max_size=10000) as cache:
        await cache.set("key", {"data": "value"})
        value = await cache.get("key")
        stats = cache.get_stats()
        print(f"命中率：{stats.hit_rate:.2%}")

asyncio.run(main())
```

### 流式搜索

```python
async with LocalSearch() as search:
    async for result in search.search_stream("TODO", path="/src"):
        print(f"{result.file_path}:{result.line_number}: {result.content}")
```

## 项目结构

```
plugins/mcps/grep-app-enhanced/
├── src/grep_app_enhanced/
│   ├── __init__.py           # 主包，包含 SearchResult 等数据类
│   ├── mcp_server.py         # MCP 服务器入口
│   ├── database/
│   │   ├── __init__.py
│   │   ├── compressed_db.py  # ZSTD 压缩数据库
│   │   └── cache_manager.py  # 缓存管理
│   ├── remote/
│   │   ├── __init__.py
│   │   ├── gh_cli.py         # GitHub CLI 封装
│   │   ├── crawler.py        # Web 爬虫
│   │   └── git_client.py     # Git 命令封装
│   └── search/
│       ├── __init__.py
│       ├── local_search.py   # 本地搜索
│       └── remote_search.py  # 远程搜索
├── scripts/
│   ├── install.sh            # 安装脚本
│   ├── setup-zstd.sh         # ZSTD 安装
│   └── test-connection.sh    # 连接测试
├── tests/
│   ├── test_database.py
│   ├── test_remote.py
│   └── test_search.py
├── pyproject.toml
└── README.md
```

## 开发

### 安装开发依赖

```bash
./scripts/install.sh --dev
```

### 运行测试

```bash
# 运行所有测试
pytest

# 运行特定测试
pytest tests/test_database.py -v

# 带覆盖率
pytest --cov=src/grep_app_enhanced --cov-report=html
```

### 代码质量

```bash
# 类型检查
mypy src/grep_app_enhanced

# 代码格式化
black src/grep_app_enhanced tests

# Lint
ruff check src/grep_app_enhanced
```

### 运行连接测试

```bash
./scripts/test-connection.sh

# 快速测试（跳过网络）
./scripts/test-connection.sh --quick

# 仅测试模块
./scripts/test-connection.sh --module
```

## 性能优化

### 压缩级别

| 级别 | 用途 | 速度 | 压缩率 |
|------|------|------|--------|
| 1-3 | 频繁写入 | 快 | 低 |
| 4-9 | 默认/平衡 | 中 | 中 |
| 10-22 | 归档数据 | 慢 | 高 |

### 缓存策略

- **内存缓存**: LRU 策略，快速访问
- **磁盘缓存**: 持久化存储，重启后保留
- **TTL**: 自动过期，避免 stale 数据

### 并行处理

```python
# 增加工作线程数（适合多核 CPU）
search = LocalSearch(max_workers=8)

# 或者通过环境变量
export GREP_APP_MAX_WORKERS=8
```

## 限制

- GitHub API 有速率限制（未认证：60/小时，已认证：5000/小时）
- 代码搜索 API 限制更严格（10 请求/分钟）
- 大仓库搜索建议使用克隆模式

## 故障排除

### ZSTD 安装失败

```bash
# 检查是否已安装
zstd --version

# 重新安装
./scripts/setup-zstd.sh --force
```

### GitHub API 认证失败

```bash
# 使用 gh CLI 认证
gh auth login

# 或者设置 Token
export GITHUB_TOKEN=ghp_xxx
```

### 内存不足

```bash
# 减少工作线程数
export GREP_APP_MAX_WORKERS=2

# 减少缓存大小
# 在代码中设置
cache = CacheManager(max_size=1000)
```

## 许可证

MIT License

## 贡献

欢迎提交 Issue 和 Pull Request！

## 相关链接

- [Oh My LiteCode](https://github.com/oh-my-litecode/oh-my-litecode)
- [MCP Protocol](https://modelcontextprotocol.io/)
- [ZSTD](https://facebook.github.io/zstd/)
