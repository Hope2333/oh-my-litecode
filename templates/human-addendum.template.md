===== HUMAN ADDENDUM =====
- Use Chinese for the final summary to me.
- Keep relay updates in the lane docs.
- Do not broaden scope beyond the active lane unless the docs clearly require it.
- If you change branch/run/PR/blocker/next-step status, update the handoff docs in the same work session.
- Keep commands, paths, commit IDs, workflow IDs, and code symbols in original English form.

Optional lines to edit when needed:
- Prioritize merge readiness over new implementation.
- Focus on log collection and blocker isolation first.
- Pause after the next green build and hand back for review.
- Prefer narrow GitHub Actions validation over long local builds when both can prove the same point.
- Keep local builds short and scoped for sanity checks or minimal repros.
- Require a fixed `Stop Reason` using one of the shared stop phrases.
- Prefer fixed fields such as `Status`, `Decision`, and `Next Action` when the prompt supports them.
- Cap the final response to 5 to 8 sections and roughly 30 to 45 lines.
- Stop after one bounded review or execution pass; do not continue recursively.
- If there is no meaningful new evidence, say so explicitly and stop.

- SEEMS STOPED, CONTINUE!!!!!!!!!
