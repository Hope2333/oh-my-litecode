# oml/oma 0.1.0 发布与验收记录（脱敏）

更新时间：2026-02-16

Scope: 本文针对 `oml/oma`（Aider）子项目 0.1.0 发布包与验收。术语见 `00-glossary-and-scope.md`。

## 1) 产物信息

- 包路径：`termux-lab/releases/oml-aider-0.1.0/stage/dist/oml-aider-0.1.0.tar.gz`
- 校验：`termux-lab/releases/oml-aider-0.1.0/stage/dist/oml-aider-0.1.0.tar.gz.sha256`
- sha256：`9b32aab285d21738914c8fdb2e0f27f4cfae2ab6e7e8de11a72f461af9de450e`

## 2) 包内敏感扫描

- 规则：`sk-[A-Za-z0-9_-]{20,}`
- 结果：0 命中

## 3) 本机验收（Linux userland）

执行：

```bash
cd termux-lab/releases/oml-aider-0.1.0/stage/oml-aider-0.1.0
make install
make doctor
```

结果：

- `launcher=ok`
- `profile=ok`
- `aliases=ok`

备注：本机未安装 aider 时，`aider=missing` 属环境前置条件问题，不影响发布包结构正确性。

## 4) 手机 Termux 验收状态（已完成）

已通过 SSH（172.18.0.1:8022）完成远端安装与验收。

### 4.1 fakehome 路径（默认）

执行：

```bash
cd ~/oml-aider-test
tar -xzf oml-aider-0.1.0.tar.gz
cd oml-aider-0.1.0
make install
make doctor
aiderx --version
```

结果：

- `launcher=ok`
- `aider=ok`
- `profile=ok`
- `aliases=ok`
- `oml_tools=ok`
- `aider 0.86.1`

### 4.2 userhome 路径

执行：

```bash
cd ~/oml-aider-test/oml-aider-0.1.0
COMMAND_NAME=aiderx-user INSTALL_TARGET=userhome make install
COMMAND_NAME=aiderx-user make doctor
aiderx-user --version
```

结果：

- `launcher=ok`
- `aider=ok`
- `profile=ok`
- `aliases=ok`
- `oml_tools=ok`
- `aider 0.86.1`

结论：`oml/oma` 已满足 0.1.0 发布门禁。

## 5) 验收命令清单（复跑用）

```bash
cd ~/oml-aider-test
tar -xzf oml-aider-0.1.0.tar.gz
cd oml-aider-0.1.0
make install
make doctor
aiderx --version

# fakehome 路径（默认）下最小调用
OPENAI_API_KEY='***REDACTED***' OPENAI_API_BASE='https://api.example.com/v1' aiderx --help >/dev/null

# userhome 路径验证
COMMAND_NAME=aiderx-user INSTALL_TARGET=userhome make install
aiderx-user --version
```
